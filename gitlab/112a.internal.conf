# server {
#     root /dev/null;
#     server_name registry.112a.internal;
#     charset UTF-8;
#     access_log /var/log/nginx/registry.112a.internal.access.log;
#     error_log /var/log/nginx/registry.112a.internal.error.log;

#     # Set up SSL only connections:
#     listen *:443 ssl http2;
#     ssl_certificate             /home/joung/code/KubeDIS/gitlab/registry.112a.internal.crt;
#     ssl_certificate_key         /home/joung/code/KubeDIS/gitlab/registry.112a.internal.key;

#     ssl_ciphers 'ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA:ECDHE-RSA-AES128-SHA:ECDHE-RSA-DES-CBC3-SHA:AES256-GCM-SHA384:AES128-GCM-SHA256:AES256-SHA256:AES128-SHA256:AES256-SHA:AES128-SHA:DES-CBC3-SHA:!aNULL:!eNULL:!EXPORT:!DES:!MD5:!PSK:!RC4';
#     ssl_protocols  TLSv1 TLSv1.1 TLSv1.2;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache  builtin:1000  shared:SSL:10m;
#     ssl_session_timeout  5m;

#     client_max_body_size        0;
#     chunked_transfer_encoding   on;

#     location / {
#         proxy_set_header  Host              $http_host;   # required for docker client's sake
#         proxy_set_header  X-Real-IP         $remote_addr; # pass on real client's IP
#         proxy_set_header  X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header  X-Forwarded-Proto $scheme;
#         proxy_read_timeout                  900;
#         proxy_pass        http://127.0.0.1:5000;
#     }
# }

server {
    listen *:5000;
    server_name  registry.112a.internal;
    charset UTF-8;
    access_log /var/log/nginx/registry.112a.internal.access.log;
    error_log /var/log/nginx/registry.112a.internal.error.log;

    location / {
        proxy_set_header  Host              $http_host;   # required for docker client's sake
        proxy_set_header  X-Real-IP         $remote_addr; # pass on real client's IP
        proxy_set_header  X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header  X-Forwarded-Proto $scheme;
        proxy_read_timeout                  900;
        proxy_pass        http://127.0.0.1:5000;
    }
}